services:
  planpush:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - planpush-data:/app/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  planpush-data:
